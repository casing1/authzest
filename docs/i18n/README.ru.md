<p align="center">
  <img src="../assets/authzest-banner.png" alt="AuthZest — анализ авторизации с учётом исходного кода" width="100%">
</p>

<p align="center">
  <a href="../../README.md">English</a> ·
  <a href="README.ko.md">한국어</a> ·
  <a href="README.ja.md">日本語</a> ·
  <strong>Русский</strong>
</p>

# AuthZest

AuthZest — устанавливаемый проект с открытым исходным кодом и основным интерфейсом CLI для анализа контроля
доступа в приложениях FastAPI по их исходному коду. Сейчас ядро на Python собирает маршруты, не импортируя
и не выполняя целевое приложение. Основная цель продукта — проверка с помощью Codex, предложения защитных
тестов и патчей, одобрение или отказ пользователя, применение только одобренных изменений и отдельно
одобренная изолированная проверка с журналом изменений. Этот процесс пока только запланирован.
Статическое сканирование остаётся офлайн, а использование Codex будет включаться явно.

Панель React — необязательный локальный интерфейс. Для использования AuthZest не нужно публиковать веб-сайт.

> [!IMPORTANT]
> Опубликованная [предварительная версия v0.1.0-alpha.1](https://github.com/casing1/authzest/releases/tag/v0.1.0-alpha.1)
> — первый запускаемый каркас проекта. Описанные ниже распознавание владельцев маршрутов, объединение
> префиксов, связывание маршрутизаторов между файлами и контракт отчёта реализованы в `main`, но **ещё не выпущены** в этих
> исполняемых файлах. Пакет из исходников также пока показывает `0.1.0a1`; используйте коммит рабочей копии
> и [историю изменений](../../CHANGELOG.md), чтобы отличить его от опубликованной версии. Ни один из вариантов
> пока не является полноценным сканером уязвимостей.

## Что поддерживает текущий исходный код

- Распознавание статически созданных объектов `FastAPI` и `APIRouter`, включая поддерживаемые псевдонимы import.
- Поиск HTTP-маршрутов со строковыми литералами и объединение поддерживаемых префиксов маршрутизатора и регистрации.
- Связывание локальных абсолютных и относительных import с сохранением исходного файла и номера строки.
- Текстовые отчёты и JSON через команду `scan`.
- Сохранение существующих полей JSON с добавлением схемы `1.2`, структурированной диагностики, статуса
  bounded/partial и отдельных ID регистраций с исходными объявлениями, app и данными `include_router`.
- Сбор непосредственно объявленных в маршруте `Depends`/`Security`, исходных позиций и известных scopes.
  Обычное внедрение зависимостей не считается доказательством аутентификации или авторизации.
- Перенос поддерживаемых объявлений зависимостей app, router и `include_router` в контекст каждой регистрации
  с сохранением отдельного списка прямых объявлений маршрута и исходных позиций.
- Необязательные локальные API и панель, диагностика и сборка отдельных исполняемых файлов.

Поддерживаются декораторы `get`, `post`, `put`, `patch`, `delete`, `options` и `head`.
Область статического анализа ограничена: неразрешённые объявления маршрутов могут быть пропущены.
Поддерживаемый маршрут с неразрешёнными сведениями о зависимостях остаётся в отчёте вместе с диагностикой.
Структурированная диагностика охватывает отдельные неразрешённые случаи и ошибки исходного кода/чтения,
но не все неподдерживаемые конструкции. Пустой отчёт или статус `bounded` не доказывает отсутствие endpoint
или безопасность контроля доступа. См. [границы парсера](../PARSER_SCOPE.md) и [контракт отчёта](../REPORT_CONTRACT.md).
Вложенные графы зависимостей, классификация аутентификации и авторизации и выявление уязвимостей пока
не реализованы. Данные эффективного контекста не гарантируют порядок выполнения зависимостей или защиту.

`scan` выполняет локальный статический анализ с отключённым адаптером Codex. Команда не вызывает Codex
и не требует API key или входа в ChatGPT. Отдельная команда `doctor` может вызывать установленный Codex CLI,
как описано ниже.

## Установка CLI из исходников

Сначала установите Python 3.12 или новее и [pipx](https://pipx.pypa.io/latest/how-to/install-pipx.html);
базовая версия для разработки — Python 3.12. Эти команды
устанавливают текущий код репозитория в изолированное окружение pipx, а не пакет из PyPI:

```bash
git clone https://github.com/casing1/authzest.git
cd authzest
pipx install --python 3.12 .

authzest --help
authzest scan /path/to/fastapi-project
authzest scan /path/to/fastapi-project --json
authzest scan /path/to/fastapi-project --json --strict
```

Замените `/path/to/fastapi-project` на каталог своего проекта. Если команда не найдена в PATH, выполните
`pipx ensurepath` и откройте новый терминал. CLI, установленный через pipx, не требует активации venv проекта.
Примеры выбирают Python 3.12. Для другого поддерживаемого интерпретатора замените `3.12` после `--python`
на его версию или путь к исполняемому файлу.

По умолчанию возвращённый отчёт даёт код выхода 0 даже при частичном анализе. С `--strict` известный
частичный анализ возвращает 1, при этом отчёт всё равно выводится. Некорректный входной путь репозитория
даёт код 2. Эти коды описывают выполнение анализа, а не оценку безопасности.

Для обновления этой установки выполните команды в рабочей копии `authzest` на `main` без незакоммиченных изменений:

```bash
git pull --ff-only
pipx install --force .
```

Готовые файлы описаны в разделе [отдельные исполняемые файлы](#отдельные-исполняемые-файлы). Extra `ui`
устанавливает Python-зависимости backend, но обычная установка wheel/pipx не включает собранные ресурсы
панели. Для необязательной панели используйте editable-установку из исходников, описанную ниже.

## Попробуйте встроенный пример

Из корня репозитория используйте установку текущего кода для сканирования локального примера:

```bash
authzest scan examples/fastapi_inventory
authzest scan examples/fastapi_inventory --json
```

Ожидаются 4 файла Python и 3 маршрута `GET`: `/health`, `/v1/catalog/items` и `/v2/catalog/items`.
Пример демонстрирует статическое обнаружение и повторную регистрацию роутера, а не поиск уязвимостей.
Структура примера и ожидаемые подтверждающие данные описаны в [руководстве по примеру](../EXAMPLES.md).

## Диагностика CLI

```bash
authzest --version
authzest doctor
authzest doctor --json
```

`doctor` проверяет среду Python. Если в PATH найден `codex`, команда также запускает `codex --version` и
`codex login status` как дочерние процессы. Она не запускает ИИ-сканирование и сама не читает файлы учётных
данных. Отсутствие Codex или входа вызывает предупреждение, но не препятствует статическому сканированию.
Успешный вход не включает ИИ-анализ: эта интеграция ещё не реализована.

## Среда разработки

Выполняйте команды из корня клонированного репозитория. Примеры shell рассчитаны на macOS/Linux. В Windows
PowerShell создайте venv командой `py -3.12 -m venv .venv` и активируйте через `.venv\Scripts\Activate.ps1`.

```bash
python3.12 -m venv .venv
source .venv/bin/activate
python -m pip install -e '.[dev]'
```

Editable-установка использует эту рабочую копию, а extra `dev` включает необязательные зависимости backend.
Node.js/npm нужны для работы над frontend, проверки документации и сборок с панелью.
В CI используется Node.js 22.

## Необязательная локальная панель

После подготовки editable-среды разработки соберите и запустите панель из корня репозитория:

```bash
npm --prefix frontend ci
npm --prefix frontend run build
authzest ui --workspace /path/to/fastapi-project --host 127.0.0.1 --port 8000
```

Откройте [http://127.0.0.1:8000](http://127.0.0.1:8000). Backend в editable-установке находит `frontend/dist`
в рабочей копии. Без сборки `/` показывает сообщение API вместо панели.

Панель различает незапущенное, пустое и частичное сканирование и показывает структурированную диагностику и подробности ошибок разбора.
При неудачном повторном сканировании прежний результат очищается. Это состояния сбора маршрутов, а не оценки контроля доступа.

Для разработки frontend в одном терминале с активным venv запустите
`authzest ui --workspace /path/to/fastapi-project --reload`, а в другом терминале из корня репозитория —
`npm --prefix frontend run dev`. Откройте [http://localhost:5173](http://localhost:5173); Vite перенаправляет
`/api` и `/health` на порт 8000.

Локальный сервер предоставляет `GET /health`, `GET /api/health`, `POST /api/scans` и документацию API в `/docs`.
Endpoint сканирования всегда использует рабочий каталог, выбранный при запуске сервера; тело запроса не
позволяет выбрать другой путь. Прямое сканирование через CLI использует путь, указанный локальным пользователем.
Запускайте необязательный сервер на loopback-адресе.

## Проверка

С активным venv разработки выполните:

```bash
python -m pytest
ruff check .
ruff format --check .
```

При изменении frontend также выполните:

```bash
npm --prefix frontend ci
npm --prefix frontend run lint
npm --prefix frontend run format:check
npm --prefix frontend run build
```

При изменении документации установите зависимости frontend, как показано выше, и выполните из корня репозитория:

```bash
node --test scripts/check_docs.test.mjs
node scripts/check_docs.mjs
git ls-files -z '*.md' | xargs -0 frontend/node_modules/.bin/prettier --check
```

Проверка сравнивает языковые пары, локальные ссылки и команды, не выполняя примеры из документации.
Форматирование проверяет Markdown, отслеживаемый Git; перед итоговой проверкой добавьте новые руководства в staging.

## Отдельные исполняемые файлы

[Опубликованная предварительная версия](https://github.com/casing1/authzest/releases/tag/v0.1.0-alpha.1)
содержит файлы для Linux x64, macOS arm64 и Windows x64, а также манифесты SHA-256. Это программы CLI,
а не графические установщики; они не включают ещё не выпущенные изменения парсера из `main`. Файлы пока
не подписаны и не нотарифицированы, поэтому ОС может предупредить о непроверенном издателе.

Для локальной сборки текущего кода активируйте venv разработки и выполните из корня репозитория:

```bash
python -m pip install -e '.[build]'
npm --prefix frontend ci
npm --prefix frontend run build
python -m PyInstaller --clean --noconfirm authzest.spec
./dist/authzest --help
```

В Windows результат находится в `dist\authzest.exe`. PyInstaller включает `frontend/dist`, если каталог
существует. Проверка тегов, контрольные суммы и публикация описаны в [руководстве по релизу](../RELEASING.md).

## Структура проекта

```text
src/authzest/
├── analyzer/   # Анализ и агрегация репозитория
├── parser/     # Разрешение маршрутов и import по AST
├── runner/     # Общий процесс сканирования
├── codex/      # Интерфейс Codex; интеграция запланирована, адаптер scan отключён
├── cli.py      # Командный интерфейс Typer
├── api/        # Необязательный транспорт FastAPI
└── models.py   # Данные отчётов ядра
tests/          # Регрессионные тесты
frontend/       # Необязательная панель React/Vite/TypeScript
docs/           # Руководства, переводы и ресурсы
scripts/        # Инструменты релиза
.github/        # Workflow CI/релиза и шаблоны для участников
```

CLI и необязательные API/UI используют одно ядро. Анализ ядра не должен зависеть от веб-сервера, React
или конкретного провайдера ИИ.

## План развития и участие

- [Указатель документации](../README.md) — руководства на английском и переводы на корейский
- [План разработки](../DEVELOPMENT_PLAN.md) и [roadmap issue #1](https://github.com/casing1/authzest/issues/1)
- [Стратегия моделей и оценки](../MODEL_STRATEGY.md)
- [Правила участия и коммитов](../../CONTRIBUTING.md)

Фиксируйте ограниченную по объёму задачу в issue, разрабатывайте её в краткосрочной ветке и отправляйте PR
с содержательными коммитами и проверками. Защищённая ветка `main` требует успешных проверок Python, frontend
и CodeQL. План курсового проекта со свободной темой отводит 7 недель на разработку, а ещё 4–5 недель —
отдельно на экзамены, задержки и итоговую подготовку. Основа отчёта и подтверждающих данных из
[#32](https://github.com/casing1/authzest/issues/32) и сведения о прямых объявлениях маршрута из
[#28](https://github.com/casing1/authzest/issues/28), а также унаследованные объявления из
[#29](https://github.com/casing1/authzest/issues/29) реализованы в текущем коде. Далее — проверка готовности
alpha.2 отдельно от публикации, затем:

1. [#33: офлайн-контракт ИИ, mock и оценка со ссылками на подтверждающие данные](https://github.com/casing1/authzest/issues/33)
2. [#35: предложения Codex, одобрение точного diff, применение одобренного патча и изолированная проверка](https://github.com/casing1/authzest/issues/35)

Эти возможности ещё не реализованы. Улучшает ли помощь ИИ результаты — гипотеза для проверки, а не доказанное
преимущество; ядро должно оставаться полезным без провайдера или фиксированной модели GPT.
Итоговая демонстрация ограничена собственным поддерживаемым fixture; передача данных, патч и выполнение
требуют отдельных разрешений. Генерация PoC для эксплуатации уязвимостей, автономные атакующие процессы
и выполнение произвольных репозиториев не входят в задачу.

## Лицензия и безопасность

AuthZest использует [лицензию MIT](../../LICENSE). Сообщайте об уязвимостях через закрытую процедуру из
[политики безопасности](../../SECURITY.md), а не через публичный issue.
